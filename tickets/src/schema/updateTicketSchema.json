{
  "type": "object",
  "properties": {
    "title": {
      "type": "string"
    },

    "price": {
      "type": "number",
      "minimum": 0,
      "maximum": 1000000
    },
    "ticketId": {
      "type": "string"
    }
  },
  "required": ["title", "price", "ticketId"]
}
